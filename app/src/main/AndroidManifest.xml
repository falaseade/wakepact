<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Alarm scheduling: SCHEDULE_EXACT_ALARM is grantable up to API 32;
         USE_EXACT_ALARM (API 33+) is auto-granted for alarm-clock apps. -->
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Ringing -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Step mission: hardware step detector needs this on API 29+ -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Pact sync (optional; solo mode works without network) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WakePactApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:enableOnBackInvokedCallback="true"
        android:theme="@style/Theme.App">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Lock-screen ring window (ADR-002). Own affinity + singleInstance so
             it never lands in the main task or recents. -->
        <activity
            android:name=".ring.RingActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance"
            android:showWhenLocked="true"
            android:taskAffinity=""
            android:turnScreenOn="true" />

        <service
            android:name=".ring.RingService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Buddy push (optional; only delivers when a Firebase project + sender
             ID are configured). Data-only messages handled in onMessageReceived. -->
        <service
            android:name=".data.pact.PactMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <receiver
            android:name=".alarmkit.AlarmReceiver"
            android:exported="false" />

        <!-- System boot broadcast reaches non-exported receivers. -->
        <receiver
            android:name=".alarmkit.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
